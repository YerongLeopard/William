#!/bin/csh
#PBS -l walltime=24:00:00
#PBS -l nodes=1:ppn=1
#PBS -N SG 
cd "$PBS_O_WORKDIR"

set jobname = SG


set PATH = /temp1/naserifa/$jobname/
cd $PBS_O_WORKDIR
hostname > run.machine

if (-d $PATH) then
   rm -r  $PATH
   mkdir $PATH
else
   mkdir $PATH
endif
cp * $PATH
cd $PATH


./exe

